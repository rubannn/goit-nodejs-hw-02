import { z } from "zod";

// Placeholder schemas — real validation rules land in Block 4 (Announcements routes).
export const announcementIdParamSchema = z.any();
export const listAnnouncementsQuerySchema = z.any();
export const createAnnouncementSchema = z.any();
export const updateAnnouncementSchema = z.any();
