import { z } from "zod";

// Placeholder schemas — real validation rules land in Block 3 (Auth routes).
export const registerSchema = z.any();
export const loginSchema = z.any();
export const refreshSchema = z.any();
